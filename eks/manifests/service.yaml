apiVersion: v1
kind: Service
metadata:
  name: std17-test-fastapi-svc
spec:
  selector:
    app: std17-test-fastapi
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: std17-test-nginx-svc
spec:
  selector:
    app: std17-test-nginx
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort